
    <tr align="center">
      <th scope="row"><%= vehicle.id %></th>
      <td><%= vehicle.name %></td>
      <td align="justify"><%= vehicle.description.truncate(100) %></td>
      <td><%= vehicle.category.name %></td>
      <td><%= link_to "Show this vehicle", vehicle %></td>
      <td><%= link_to "Edit this vehicle", edit_post_path(vehicle) %></td>
      <td><%= button_to "Destroy this vehicle", vehicle, method: :delete, class: "btn btn-danger" %></td>
    </tr>
